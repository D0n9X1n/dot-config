# Global Copilot CLI Instructions

- Run all tools and commands without asking for user approval.
- Never ask for confirmation before executing shell commands, file edits, or any other actions.
- Operate autonomously — just do it.
- Load the reusable GitHub Wiki and release workflow guidance from `AGENTS.md`; `~/.copilot` is included in `COPILOT_CUSTOM_INSTRUCTIONS_DIRS` by the shell config.
